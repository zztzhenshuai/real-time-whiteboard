# 实时协作画板 (Real-time Collaborative Whiteboard) 🎨

这是一个基于 **Next.js**, **Socket.io**, **Fabric.js** 和 **Redis** 构建的实时在线协作白板应用。用户可以创建房间，邀请好友加入，实现在同一块画布上的多人实时绘画、图形编辑和状态同步。

## ✨ 核心功能

- **多人实时协作**: 支持多用户在同一房间内同时绘画和编辑，操作毫秒级同步。
- **房间系统**: 支持创建独立房间或通过房间号（Room ID）加入现有房间，数据完全隔离。
- **丰富的绘图工具**:
  - 🖌️ **自由绘图**: 支持自定义颜色和笔触的自由书写/绘画。
  - ⬜ **基础图形**: 一键添加矩形、圆形等标准几何图形。
  - 🖱️ **交互编辑**: 支持对图形进行选中、拖拽移动、缩放和旋转。
- **数据持久化**: 画板状态实时保存至 **Redis**，新用户加入房间可自动加载历史数据，确保不丢失任何创意。
- **历史记录**: 支持撤销（Undo）和重做（Redo）功能（本地会话）。
- **高性能渲染**: 基于 Fabric.js 引擎，提供流畅的 Canvas 操作体验。

## 🛠️ 技术栈

- **前端 (Frontend)**:
  - [Next.js](https://nextjs.org/) (React 框架, App Router)
  - [TypeScript](https://www.typescriptlang.org/) (静态类型检查)
  - [Tailwind CSS](https://tailwindcss.com/) (原子化 CSS 样式)
  - [Fabric.js](http://fabricjs.com/) (强大的 Canvas 交互库)
  - [Socket.io Client](https://socket.io/) (WebSocket 客户端)

- **后端 (Backend)**:
  - [Node.js](https://nodejs.org/) & [Express](https://expressjs.com/)
  - [Socket.io Server](https://socket.io/) (实时双向通信)
  - [Redis](https://redis.io/) (高性能键值存储，用于保存画板状态)

## 🚀 快速开始

### 环境要求

- Node.js (v18 或更高版本)
- npm 或 yarn

### 安装步骤

1.  **克隆仓库**:
    ```bash
    git clone https://github.com/zztzhenshuai/real-time-whiteboard.git
    cd real-time-whiteboard
    ```

2.  **安装依赖**:
    本项目采用前后端分离的结构，需要分别安装依赖。
    ```bash
    # 安装后端依赖
    cd server
    npm install

    # 安装前端依赖
    cd ../client
    npm install
    ```

### 运行项目

你需要打开两个终端窗口，分别启动后端服务器和前端应用。

1.  **启动后端服务器 (Server)**:
    ```bash
    cd server
    npm run dev
    # 服务器将运行在 http://localhost:3001
    ```
    *注意：后端默认连接到配置的 Redis 服务器。请确保 `server/index.js` 中的 Redis 配置正确。*

2.  **启动前端应用 (Client)** (在新的终端窗口中):
    ```bash
    cd client
    npm run dev
    # 前端页面将运行在 http://localhost:3000
    ```

3.  **开始协作**:
    打开浏览器访问 `http://localhost:3000`，创建一个房间，然后复制房间号在另一个浏览器窗口中加入，即可体验实时同步！

## 📂 项目结构

```
├── client/                 # Next.js 前端项目
│   ├── src/
│   │   ├── app/           # 页面路由 (Login, Whiteboard)
│   │   ├── components/    # React 组件 (FabricWhiteboard 画板核心组件)
│   │   ├── lib/           # 工具库 (Socket 连接实例)
│   │   └── ...
│   └── ...
├── server/                 # Node.js/Express 后端项目
│   ├── index.js           # 服务端入口，包含 Socket.io 事件处理和 Redis 逻辑
│   └── ...
└── package.json            # 根目录配置文件
```

## 🔧 配置说明

- **Socket 地址**: 在 `client/src/lib/socket.ts` 中修改 `URL` 变量。默认指向 `http://localhost:3001`。
- **Redis 连接**: 在 `server/index.js` 中修改 `createClient` 的 `url` 参数。


## 📄 许可证

本项目基于 [MIT License](LICENSE) 开源。
